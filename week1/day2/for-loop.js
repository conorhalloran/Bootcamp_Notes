A for loop requires multiple arguments
A for loop has:
// Variable initilization
// condition
// increment (code executes after)

// for (<variable initilization>; <condition>; <increment>)
for(let i = 0; i < 5; i += 1) {
  console.log('Mambo No:', i);
}

Print all multiples of 3 on the screen up to 100

for(let i = 0; i < 100; i += 3) {
  console.log(i);
}
